version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npx tsc --noEmit
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'