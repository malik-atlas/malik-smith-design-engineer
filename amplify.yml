version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build # Use 'dist' if using Vite, 'build' if using Create React App
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*